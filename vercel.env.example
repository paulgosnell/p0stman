# Vercel Environment Variables for ElevenLabs Webhook
# Add these to your Vercel project settings:
# https://vercel.com/YOUR-USERNAME/p0stman/settings/environment-variables

# Required (get from Supabase Settings → API → service_role key)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# Required (get from ElevenLabs Dashboard → Conversational AI → Settings → Webhooks)
ELEVENLABS_WEBHOOK_SECRET=your_elevenlabs_webhook_secret_here

# Optional (get from https://api.slack.com/messaging/webhooks)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Instructions:
# 1. Replace values above with your actual credentials
# 2. Add each to Vercel project settings
# 3. Set Environment: Production, Preview, Development (all three)
# 4. Click "Redeploy" in Vercel dashboard
